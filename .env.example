# Local development configuration for `dev-server.mjs`
#
# 1) Copy this file to `.env`
# 2) Fill in values below
#
# Notes:
# - Never commit real tokens/credentials.
# - `MAILTRAP_API_TOKEN` is used server-side only (safe from the browser).

# Dev server
HOST=127.0.0.1
PORT=5173

# Mailtrap Email Sending API
# Get a token from Mailtrap -> Email Sending -> API Tokens
MAILTRAP_API_TOKEN=REPLACE_ME

# Required "From" identity
MAILTRAP_FROM_EMAIL=orders@example.com
MAILTRAP_FROM_NAME=Inventory Manager

# Optional (defaults to https://send.api.mailtrap.io/api/send)
# MAILTRAP_API_BASE_URL=https://send.api.mailtrap.io/api/send

# Optional safety: restrict recipients by domain (comma-separated)
# ALLOWED_RECIPIENT_DOMAINS=example.com,acme.com

